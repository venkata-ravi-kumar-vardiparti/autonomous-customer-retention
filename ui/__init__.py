"""ChurnGuard's Streamlit UI. Not part of the churnguard/ installed package
(see repo layout in CLAUDE.md) - lives at the repo root next to src/ and
tests/, importable in tests the same way the tests/ package itself is
(repo root on sys.path)."""
